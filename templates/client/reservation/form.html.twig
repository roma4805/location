{% extends 'base.html.twig' %}

{% block title %}Réserver {{ voiture.marque }} {{ voiture.modele }}{% endblock %}

{% block body %}
<style>
/* 🌌 Page réservation sombre et moderne */
:root {
    --bg-main: #0b1220;        /* fond général sombre */
    --bg-card: #1a1f33;        /* fond du formulaire */
    --accent-gradient: linear-gradient(135deg, #b91c1c, #ef4444, #fb7185); /* rouge moderne */
    --text-main: #e5e7eb;      /* texte principal */
    --text-muted: #94a3b8;     /* texte secondaire */
    --input-bg: #131b2c;       /* champs de formulaire sombre */
    --input-border: #2c3e50;
}

/* ===== PAGE ===== */
.booking-page {
    background: var(--bg-main);
    min-height: 100vh;
    padding: 50px 20px;
    font-family: 'Poppins', sans-serif;
    color: var(--text-main);
}

/* ===== FORMULAIRE ===== */
.booking-container {
    max-width: 600px;
    margin: 0 auto;
    background: var(--bg-card);
    padding: 40px 30px;
    border-radius: 20px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.5);
    color: var(--text-main);
}

.booking-container h1 {
    font-weight: 700;
    font-size: 2rem;
    text-align: center;
    margin-bottom: 30px;
    background: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

/* ===== CHAMPS ===== */
.form-group {
    margin-bottom: 20px;
}

.form-control {
    width: 100%;
    border-radius: 12px;
    padding: 12px 15px;
    border: 1px solid var(--input-border);
    background: var(--input-bg);
    color: var(--text-main);
    transition: all 0.3s ease;
}

.form-control:focus {
    border-color: #ef4444;
    box-shadow: 0 0 8px rgba(239,68,68,0.3);
    outline: none;
    background: #1f2433;
}

/* ===== BOUTON RÉSERVER ===== */
.btn-reserve {
    display: block;
    width: 100%;
    background: var(--accent-gradient);
    color: white;
    font-weight: 600;
    padding: 12px;
    border-radius: 50px;
    border: none;
    font-size: 1.1rem;
    transition: all 0.3s ease;
    cursor: pointer;
}

.btn-reserve:hover {
    opacity: 0.9;
    transform: translateY(-2px);
}

/* ===== NOTE ===== */
.note {
    margin-top: 20px;
    text-align: center;
    font-size: 0.95rem;
    color: #fefefe;
    padding: 15px;
    background: rgba(239,68,68,0.1);
    border-radius: 12px;
    border-left: 5px solid #ef4444;
}

/* ===== RESPONSIVE ===== */
@media(max-width:768px){
    .booking-container {
        padding: 30px 20px;
    }
}
</style>

<div class="booking-page">
    <div class="booking-container">
        <h1>Réserver {{ voiture.marque }} {{ voiture.modele }}</h1>

        {{ form_start(form) }}

        <div class="form-group">
            {{ form_row(form.dateDebut) }}
        </div>
        <div class="form-group">
            {{ form_row(form.dateFin) }}
        </div>
{# ===== MODE DE PAIEMENT ===== #}
<div class="form-group">
    <label style="display:block; margin-bottom:10px; font-weight:600;">
        Mode de paiement
    </label>

    <div style="
        background: var(--input-bg);
        border: 1px solid var(--input-border);
        border-radius: 12px;
        padding: 15px;
    ">
        <label style="display:flex; align-items:center; gap:10px; cursor:pointer;">
            <input type="radio" name="paiement" value="cash" checked
                   style="accent-color:#ef4444;">
            <span>
                💵 <strong>Paiement en espèces (Cash)</strong><br>
                <small style="color:var(--text-muted);">
                    Payez directement lors de la récupération du véhicule
                </small>
            </span>
        </label>
    </div>
</div>

        {# Bouton personnalisé #}
        <button type="submit" class="btn-reserve">Réserver</button>

        {# Rend uniquement le reste des champs cachés (CSRF token, etc.) #}
        {{ form_rest(form) }}

        {{ form_end(form, {'render_rest': false}) }}

        <div class="note">
            ✨ Astuce : Réservez cette voiture pour plus d’un mois et profitez d’une remise spéciale !  
            C’est le moment idéal pour planifier vos voyages en toute tranquillité. 🚗💨
        </div>
    </div>
    
</div>
{% endblock %}
